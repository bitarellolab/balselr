# An example VCF (variant call format) read into R

An example VCF (variant call format) read into R

## Usage

``` r
vcf_obj
```

## Format

A data.table with 838 rows and 128 columns

## Source

created from a VCF (Variant Call Format) input file generated with the
SLiM simulator by reading it in with read_vcf().
