# source("renv/activate.R")
if (interactive()) {
  suppressMessages(require(devtools))
}

options(
  usethis.full_name = "Bárbara Bitarello",
  usethis.description = list(
    `Authors@R` = 'c(person("Bárbara", "Bitarello", email = "bbitarello@brynmawr.edu", role = c("aut", "cre"),
    comment = c(ORCID = "0000-0001-7676-9367")),
    person("Daphne", "Hansell",role = c("aut")))',
    License = "",
    Version = "0.0.0.1000"
  ),
  usethis.protocol = "ssh"
)
options(rstudio.help.showDataPreview = FALSE)
